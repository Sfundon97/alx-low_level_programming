#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - check the last digit
 * Return: 0
 */
int main(void)
{
	int n = 97;

	while (n <= 122)
	{
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
