#include "main.h"
/**
 * _isupper - check for uppercase
 *  @c: the number to be checked
 *  Return: 1 if it is uppercase or 0 if not
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 98)
	{
		return (1);
	}
	else
	{
return (0);
	}
}
